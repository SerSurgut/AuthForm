# AuthForm

Мини-проект формы авторизации с двухфакторной аутентификацией (2FA), построенный на React, TypeScript и Ant Design.

## 📋 Описание

Проект представляет собой многошаговую форму входа, которая включает:

- **Шаг 1**: Ввод email и пароля
- **Шаг 2**: Ввод 6-значного кода подтверждения из Google Authenticator

Форма сохраняет введенные данные при переключении между шагами и поддерживает возврат на предыдущий шаг без потери данных.

## 🚀 Технологии

- **React 19** - библиотека для построения пользовательского интерфейса
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик и dev-сервер
- **Ant Design 5** - UI библиотека компонентов
- **CSS Modules** - модульные стили

## 📦 Установка

1. Клонируйте репозиторий или скачайте проект
2. Установите зависимости:

```bash
npm install
```

## 🛠️ Запуск проекта

### Режим разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
```

### Превью продакшен сборки

```bash
npm run preview
```

### Линтинг

```bash
npm run lint
```

## 📁 Структура проекта

```
src/
├── api/
│   └── mock.ts                 # Мок API для тестирования
├── components/
│   ├── Logo/                   # Компонент логотипа
│   └── SignIn/                 # Основной компонент формы авторизации
│       ├── components/
│       │   ├── LoginStep/       # Компонент шага входа (email/password)
│       │   ├── SignInForm/     # Обертка формы
│       │   └── VerifyCodeStep/  # Компонент шага верификации кода
│       ├── context/             # React Context для управления состоянием
│       │   ├── AuthForm.context.tsx
│       │   └── AuthForm.provider.tsx
│       ├── services/            # Сервисы для работы с API
│       │   └── signInService.ts
│       ├── utils/               # Утилиты и константы
│       │   ├── constants.ts
│       │   └── errorHandler.ts
│       ├── validation/          # Правила валидации полей
│       │   └── rules.ts
│       ├── types.ts             # TypeScript типы
│       └── SignIn.tsx           # Главный компонент
├── App.tsx                      # Корневой компонент приложения
└── main.tsx                     # Точка входа
```

## 🎯 Основные возможности

### Валидация полей

- **Email**: проверка формата email адреса
- **Password**: минимум 6 символов
- **Code**: ровно 6 цифр

### Сохранение данных формы

Форма автоматически сохраняет введенные данные при переключении между шагами благодаря:

- Свойству `preserve` в Ant Design Form
- Сохранению значений на уровне каждого поля

### Обработка ошибок

Проект включает обработку различных типов ошибок:

- Неверные учетные данные (401)
- Заблокированный пользователь (403)
- Ошибки сервера (500)
- Сетевые ошибки

### Многошаговая навигация

- Переход на следующий шаг после успешной валидации
- Возврат на предыдущий шаг с сохранением данных
- Визуальная индикация текущего шага

## 🧪 Тестирование с мок API

Для тестирования различных сценариев используйте следующие данные (да, я знаю, что пароль `123456` - это плохая практика, но это же мок! 😅):

### Успешный вход

- **Email**: любой (кроме специальных случаев ниже)
- **Password**: `123456`
- **Code**: `123456`

### Специальные тестовые случаи

#### Заблокированный пользователь

- **Email**: `blocked@example.com`
- **Password**: любой

#### Ошибка сервера

- **Email**: `server@example.com`
- **Password**: любой

#### Сетевая ошибка

- **Email**: `network@example.com`
- **Password**: любой

#### Неверный пароль

- **Email**: любой
- **Password**: любой (кроме `123456`)

#### Неверный код

- **Code**: любой (кроме `123456`)

## 🎨 Стилизация

Проект использует CSS Modules для изоляции стилей компонентов. Каждый компонент имеет свой файл стилей:

- `LoginStep.module.css`
- `SignInForm.module.css`
- `VerifyCodeStep.module.css`
- `Logo.module.css`

## 📝 Основные компоненты

### AuthFormProvider

Провайдер контекста, который управляет:

- Текущим шагом формы
- Состоянием загрузки
- Валидацией полей
- Обработкой логина и верификации кода

### LoginStep

Компонент первого шага с полями:

- Email (с иконкой пользователя)
- Password (с иконкой замка)
- Кнопка "Log in"

### VerifyCodeStep

Компонент второго шага с:

- OTP полем для ввода 6-значного кода
- Автоматической валидацией при вводе полного кода
- Кнопкой "Continue" (появляется после успешной валидации)

## 🔐 Безопасность

⚠️ **Важно**: Это демонстрационный проект с мок API. В реальном приложении используйте реальную аутентификацию на бэкенде

## 📄 Лицензия

Этот проект создан в образовательных целях.
